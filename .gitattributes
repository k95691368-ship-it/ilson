# 시연용 정산서는 손대지 않는다.
#
# 03_쿠팡 파일은 CP949로 저장돼 있고, 02·05는 엑셀(압축 파일)이다.
# git이 줄바꿈을 바꾸면 파일이 달라지고, 그러면 "이 파일을 그대로 읽어냈다"는
# 주장이 흔들린다. 바이너리로 못 박아 어느 컴퓨터에서 받아도 같은 바이트가
# 되게 한다.
public/samples/** binary
seed/out/** binary
