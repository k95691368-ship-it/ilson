# 로컬 개선 진행 기록

## 자율 개선 회차 8 — 신규 과제의 근거·서명·인계 연결 (2026-09-26)

직전 회차는 93dd1ab3372fc558d9af53de59cdfe97dad6435e로 푸시했고 GitHub CI 36218680756이 통과했다. Pages ilson의 CLI 배포 46d5f6ef-0d5b-44bb-be23-f62c16b2adf0, health 200/ready, 운영 격리 API 29개와 로컬 산출물 SHA256 일치를 확인했다. 임시 자료와 서버는 정리했으며 portfolio는 변경하지 않았다.

이번에는 새 신청을 실제 PostgreSQL API로 생성해 확인했다. 기존 화면에는 기준·실측 입력과 인계 생성 경로가 없어 베타 뒤 도구 실행·성과 확인을 완료할 수 없었다. 더 큰 대시보드 복원 대신 기존 협의 화면에 접힌 입력 2개, 베타 화면에 접힌 인계 1개만 보완했다. 자동 코드 생성·새 정산 엔진·외부 AI 호출·강제 자동 승인은 제외했다. 인계 대상은 지원 기간·시연 상품·고정 환율이 명시된 기존 브라우저 정산 도구이며 원본 파일은 서버에 저장하지 않는다.

실행으로 재현한 보안·정확성 문제는 오래된 기준 PATCH 덮어쓰기, ID만 같은 변경 기준에 대한 옛 서명 재사용, 응답 유실 재요청 중복, 측정값의 boolean/배열 숫자 강제 변환, 봉인된 실측 원본 삭제, 저장 직전 작성 권한 회수였다. 기준·기준선 판본과 최신 권한을 원자 저장에서 비교하고 감사와 영수증을 함께 남긴다. 서명은 기준 전체 및 revision에 연결하며 과거 원문은 보존한다. 베타 회차의 서버 소유 revision을 보존하여 기준을 바꿨다가 되돌리는 경우도 재시험한다. 인계에는 현재 기준·기준선·관련 부서 확인·최신 제작/베타·미해결 문제를 검사하고 사람 기준은 별도 확인 근거를 받는다. 중단 후 재개는 새 베타가 필요하며 이전 수령과 신청 상태를 새 수령인 것처럼 바꾸지 않는다. DB 스키마 변경은 없다.

독립 검토에서 충돌 재조회 후 미편집 안전 설정을 옛값으로 덮는 UI와 참여 해제된 부서의 옛 서명이 인계를 막는 경계를 추가 재현했다. 필드별 변경 추적·미편집 최신값 병합·같은 필드 충돌 시 명시 선택으로 보완했고, 서명 판정은 현재 필수 부서만 사용한다. 응답 유실은 같은 본문으로만 재시도하며 신청/계정 전환 후 늦은 응답은 새 입력을 건드리지 않는다. 전체 검사의 접근성 오류 연결과 이벤트 개수 불일치를 수정했고, 브라우저에서 발견한 8개 진행 항목을 ‘여섯 단계 중 7칸’으로 표시하는 문구도 실제 항목 수로 고쳤다.

신규 신청부터 합격 기준·실측 3회(580/600/620초, 중앙값600)·직접 부서 서명·브라우저 정산·베타 통과·사용법 인계·도구 실행·수령·직접 성과 확인까지 로컬 화면에서 실행했다. 합성 CSV 142줄/검토5줄의 계산·실행 기록은 새로고침 후 유지됐다. 협의·인계는 390/1280/1920px에서 문서 가로 넘침이 없었고 CSS 산출물 index-BRFuXbZt.css는 이전과 같다. 중단 후 재개 버튼은 새 베타가 없어 비활성임을 확인했다. API 격리 smoke 57개는 잘못된 scope409, stale 기준409, 인계 영수증 재사용, 초기화 후 옛 쿠키401·시험 신청404까지 통과했다. 별도 피드백→실험→재평가·정산 감사 5파일119개에서 새 필수 결함 근거는 없었다.

최종 코드에서 lint→build→test를 순서대로 실행해 모두 종료 0이었다. 전체 150파일2,324개(일반2,318개+규모6개)가 통과했고 클라이언트125모듈과 Worker가 빌드됐다. npm audit의 알려진 의존성 취약점은 0건이며 최종 서버의 격리 API smoke 57개도 다시 통과했다. 같은 커밋의 CI 통과 후 CLI로 배포하고 운영 health·산출물·격리 시험 공간을 확인한다. 배포 결과와 커밋은 작업 응답 및 서비스 배포 이력으로 남긴다. 운영 침투 시험·실사용 생산성·다중 DB 세션 부하 및 장기 성능 효과는 검증하지 않았으며 합성 값을 실제 절감 성과로 주장하지 않는다. 새 근거 없이 기능 개수를 늘리는 후보는 폐기한다.

## 자율 개선 회차 7 — 현재 상태 재검증 (2026-09-26)

사용자는 일손의 진단·필수 기능 판단·구현·검증·커밋·푸시·배포를 사전 승인했다. 아래의 과거 회차별 ‘배포 제외’는 당시 범위의 기록이며 이번 회차의 제한이 아니다. 시작 HEAD는 4351fa1이고 작업 트리는 깨끗했다. origin은 k95691368-ship-it/ilson, 브랜치는 main, Pages 설정은 ilson, 운영 health는 HTTP 200 및 Supabase/db/schema/runtime/capacity 정상이다. 현재 npm audit의 알려진 의존성 취약점은 0건이다. 기존 디자인·원본 로컬 처리·권한·격리를 유지하며 portfolio는 제외한다.

독립된 읽기 검토 3개와 실제 메모리 PostgreSQL·Node 재현으로 필수 후보를 선정했다. 정산 담당자가 오류가 섞인 파일을 처리할 때 할인액 ‘미확인’이 0으로 계산되고, 2026-06-31이 정상 날짜로 처리되며, 상속 속성명 상품코드가 전체 실행을 중단하고, 매우 큰 금액이 Infinity/NaN으로 변했다. 이는 실행으로 확인한 사실이고 잘못된 보고·정상행 처리 중단은 그에 따른 위험 추론이다. 새 수동 검토 화면 대신 기존 예외 격리와 원본 근거를 보완한다. 정상 골든셋·반품·빈 할인 정책 유지, 잘못된 행 격리, 정상행 계속 처리, 유한하고 안전한 금액 집계를 합격 기준으로 한다.

성과 담당자·확인 부서의 경로에서는 실제 API로 운영비 2,000원을 추가해 순금액이 2,276원에서 276원으로 바뀌어도 ‘확인됨’과 이전 부서 확인이 유지됐다. 음수 공수·운영비도 저장됐고, 기준선·실행이 없는 신청의 직접 확인은 화면에서 불가여도 POST 200이었다. 새로운 성과 승인 대시보드는 사용 부담에 비해 근거가 부족하여 제외하고 기존 성과·부서 확인 경로를 보완한다. 수치·근거 버전에 결합한 확인, 과거 감사 보존, 잘못된 입력 거절, 오래된 화면 저장 충돌, 재조회 일치를 합격 기준으로 한다.

문제 담당자 두 명의 편집에서는 B가 원인 확정·해결 상태를 저장한 뒤 A의 오래된 폼이 POST 200으로 과거 미확정·미해결 상태를 복원했다. 기존 트랜잭션은 요청 시작 이후의 경쟁만 확인하므로 오래 열린 폼을 보호하지 못한다. 별도 잠금·편집 예약 기능 대신 사용자가 본 원본 버전 사전조건과 기존 원자 저장을 결합하는 대안을 선택한다. 선행 저장 보존, 409에서 초안 유지와 최신 자료 재확인, 동일 원인 편집 경계 검사를 합격 기준으로 한다.

위 세 항목을 기존 화면·API 안에서 보완했다. 성과의 현재 근거·입력·확인·해소를 공통 함수로 계산하며, 과거 감사 기록은 지우지 않고 현재 유효 여부와 구분한다. 성과·현황·응답률·정직·부서·도구·조회·인쇄 기록 8개 소비처의 일치를 실제 PostgreSQL 호환 실행으로 확인했다. 요약 경로는 실행 10,000행 대신 집계 1행과 원본 지문을 반환하고 상세 경로와 계산값·지문이 같음을 확인했다. 직접 부서 확인, 대리 확인, 이전 확인을 구분하며 음수 비용·잘못된 수·근거 없는 확인을 거절한다.

독립 검토에서 요청 인증 후 저장 직전 부서 권한을 회수해도 확인 기록이 남는 경쟁 조건을 실제 RPC로 재현했다. 현재 계정 권한을 원자 저장의 읽기 집합에 넣고 성과·기준 서명·수령/거절·보류 해소/취소·시험판 의견에 적용했다. 선언과 감사 기록을 함께 저장하고 성공 영수증 재시도는 중복을 만들지 않는다. 신규 18개 실제 PG 검사는 권한 회수·정상 상태·중복 방지·감사 실패 전체 롤백을 검증했다. 별도 읽기 검토에서 지정 범위의 추가 수정 사항은 없었다. DB 마이그레이션이나 운영 자료 변경은 필요하지 않았다.

최종 코드에서 lint → build → test를 순서대로 실행해 모두 종료 0이었다. 전체 145파일 2,277개(일반 2,271개 + 규모 6개)가 통과했고 클라이언트 122모듈과 Worker가 빌드됐다. npm audit의 알려진 의존성 취약점은 0건, diff 공백 오류 0건이다. 중간 전체 실행의 6개 실패는 변경된 버전 계약·모의 DB 원자 저장·이벤트 종류 수 검사였으며 계약에 맞춰 보완한 후 전체를 재실행했다. 테스트나 권한 조건은 제외하지 않았다. CSS 산출물은 index-BRFuXbZt.css로 기존 디자인과 동일하다.

실제 로컬 브라우저 두 탭에서 먼저 저장한 원인·판정 보존, 오래된 편집 409, 초안 유지, 최신 자료 명시 확인 뒤 재저장을 실행했다. 390/1280/1920px에서 가로 넘침이 없었다. 성과는 의견 불일치의 빈 시간 거절과 명시적 0, 비용 변경 후 이전 확인 무효화, 충돌 후 최신 수치 조회·초안 보존·오류 제거·재확인·새로고침을 검증했다. 최종 서버에서 수령과 합격 기준 서명도 저장·재조회했다. 정산 합성 CSV 7행은 정상 3행(순매출 14,500원·수수료 464원·공헌이익 4,566원)과 오류 4행으로 분리됐고 원본 줄 번호가 보존됐다. 저장 응답 유실 뒤 동일 실행 재시도에서 총 실행 2회(기존 실패 1 + 새 성공 1)로 중복이 없었다. 합성 CSV는 삭제했다. 개인 체험 두 공간의 저장·충돌·격리·실측/기준선·초기화 API 29개 확인도 최신 로컬 서버에서 통과했다.

이 기록 시점에는 커밋·CI·원격 배포가 남아 있다. origin/main과 Pages ilson 대상을 다시 확인했고 portfolio는 건드리지 않는다. 같은 커밋의 CI 통과 후 CLI로 배포하며 운영 health·산출물·격리 시험 공간을 확인하고 시험 자료와 임시 서버/파일을 정리한다. 배포 결과와 커밋은 작업 응답 및 서비스 배포 이력으로 남긴다. 운영 침투 시험·다중 DB 연결 부하·실사용 절감 효과는 검증하지 않았다. 대리 확인의 브라우저 prompt는 검증 브라우저가 지원하지 않아 UI 경로를 완료하지 못했으며 API/화면 회귀 검사로만 확인했다.

후속 후보를 반박 검토한 결과, 일반 신규 신청의 기준선 실측 입력과 운영 건수 편집 진입 UI는 현재 없지만 해당 화면을 의도적으로 제거한 코드 기록이 있다. 기준선은 기존 API로만 봉인할 수 있으므로 신규 신청의 성과 확인을 화면만으로 완주하는 것은 이번에 해결했다고 주장하지 않는다. 삭제된 화면의 재도입은 이번 최적화·보안 배포에 섞지 않았고, 새로운 대시보드·잠금 예약·수동 정산 검토 기능도 기존 경로 보완으로 충분하여 제외했다.

## 회차 1 — 필수 개선 로컬 반영 확인 (2026-09-19)

이번 반복 목표의 시작점은 기존 로컬 변경 사항이다. 이전 회차에서 간편 제보, 개인 담당자·회신 일정, 후속 과제, 행동 권한·데이터 범위·계정 회수·외부 연동 재시도를 구현했으며, 당시 전체 테스트 1,524개와 빌드·린트를 실행했다. 이 기록은 이전 결과이며 현재 회차의 완료 증거로 재사용하지 않는다. 기존 사용자 변경 사항과 운영 데이터는 보존하고, 커밋·푸시·원격 배포는 하지 않는다.

현재 코드와 README에서 확인한 핵심 사용자는 사내 AI 도구를 사용하는 직원과 개선 담당자이다. 신청·검토·협의·제작·베타·성과 및 현장 피드백·문제 분석·실험·재측정의 연결을 유지한다. 이번 회차는 코드 검토와 PostgreSQL 재현을 통해 필수 문제를 최대 5개 선정하는 단계다. 후보는 오래된 사건의 새 안내 접근, 근거 부족 표본의 재검토, 권한 범위와 공유 문제 집계의 표시 일관성, 참여 부서 접근, 사용량 제한의 조회·복구 일관성이다. 아직 재현을 완료하지 않은 후보를 확정된 결함으로 취급하지 않는다.

작업 시작 시 work/progress.md와 로컬 5187 리스너는 없었다. 회차 종료 조건에는 빌드, 실제 API를 사용하는 로컬 서버 실행, 브라우저 동작 검증, 검증용 데이터 정리를 포함한다. 이전 테스트용 프로세스를 살아 있다고 가정하지 않는다. 사용량 조회는 일반 사용 가능, 주간 사용률 46%를 반환했다. 사용량은 조회 시점의 계정 값이며 잔여 토큰 수로 환산하지 않는다.

독립 읽기 검토와 메모리 PostgreSQL 재현으로 다섯 항목을 선정했다. 첫째, 본인 제보 101건 중 가장 오래된 제보에 새 안내를 등록하면 unread=1이나 cases 100건에 안내가 없었다. 생성순 커서 탐색을 추가하며, 최신 활동순만 바꾸는 방법은 미확인 100건 초과를 해결하지 못해 제외한다. 둘째, insufficient 판정 뒤 근거를 확보해도 재점검은 409, 재추출은 400이었다. insufficient만 비종결로 다루고 이전 판단은 추가 이력으로 보존한다. 셋째, 직원별 GET에서 공유 문제 2건이 1건으로 축소되면서 비용·우선순위는 공유 값으로 남았다. 원문 접근 범위는 좁힌 채 기존 보호된 집계 함수로 전체 문제 건수를 계산하고 열람 가능한 원문 수를 별도로 표시한다. 넷째, 참여 부서의 부서 자격 검사는 통과해도 해당 신청 조회가 404였다. 자유 입력 로그를 권한으로 해석하지 않고 인증된 담당자가 명시적으로 등록한 참여 범위를 사용한다. 다섯째, 실제 한도가 소진되어도 remainingToday=1이며 실패 반환 뒤 hit 수가 1로 유지되었다. 일반 제한 테이블을 개방하지 않고 전용 RPC에서 버킷·티켓·활성 계정을 검증한다.

우선순위는 업무가 멈추는 참여 부서 접근(P1, 중간 규모)이며, 안내 접근·재점검·집계·한도 일치(P2, 소규모~중간 규모)를 병렬 보완한다. 회귀 검증은 실제 scoped PostgreSQL 핸들러와 화면 상호작용을 결합한다. 정상 부서와 제외 부서, 100건 경계, 확정 판정의 덮어쓰기 금지, 이전 증거 보존, 다른 사람 원문 비노출, 한도 소진·반환·다른 티켓 거부를 완료 기준으로 둔다. root는 집계·로컬 실행·통합 검증을, 독립 작업은 feedback 범위와 participation/quota 범위로 분리한다. migration loader와 health 통합은 root만 수정한다.

집계 구현은 overrideMetrics의 보호된 공유 집계와 별도 열람 가능 건수, OverridePage의 원문 접근 범위 안내로 반영했다. 현재 마이그레이션 초안을 포함한 accessScopePostgres·essentialWorkflowUi·auditHardeningPostgres·httpBoundaryHardening 4파일 60개 테스트가 통과했다. 이는 전체 테스트·화면 검증의 대체가 아니다. 0008·0009 로더 및 readiness/health 검사 연결을 진행했고, 표본 묶음도 LIMIT 50 뒤에는 재점검 경로가 없는 동일 유형 문제를 추가 발견하여 재점검 지속성 항목에서 함께 보완 중이다.

피드백·표본 묶음의 독립 커서, 부족 판정 재검토와 DB append-only 이력, 관리자 참여 부서 명시 확인 화면, 계정별 도구 한도 조회·반환을 반영했다. 실제 화면 진입 중 확인한 부수 회귀(GET /demo/workspace 비활성 조회까지 차단)를 수정했으며 나머지 데모 쓰기·인증 보호는 그대로 검증했다. 1차 전체 검사에서는 1,567개 중 1개가 미사용 응답 필드 2개를 발견했다. 불필요한 값은 제거하고 원본 집계 범위를 인사이트에 표시해 재검사 중이다. 부족 판정을 최종 완료처럼 읽지 않도록 수치 제목도 ‘점검한 표본’으로 고쳤다.

localhost:5188 가상 계정 검증 서버(세션 72682)에서 브라우저로 참여 권한 명시 확인, 101번째 제보 안내 열기·읽음(1→0)·미해결 후속 과제 생성, 51번째 점검 묶음의 insufficient→issue와 이전 이력 2회 표시를 실행했다. 원본 스냅샷은 그대로 표시되며 최종 판정 입력창이 사라졌다. 390px 화면에서 문서 너비 375px로 가로 넘침이 없었고 페이지 이동 버튼과 본문 배치를 확인했다. 가상 계정·서명키·기록은 메모리 전용이며 종료 시 모두 소멸한다. tests/fixtures/localAccess.mjs는 같은 경계를 재현하는 재사용 검증 소스이고 임시 사용자 데이터가 아니다.

최종 검증은 전체 99파일 1,574개 테스트 통과(일반 1,568개 + 규모 6개), 마지막 문구 변경 관련 3파일 19개 재검사, 전체 린트와 클라이언트·Worker 빌드 성공이다. scripts/dev-demo.mjs를 최신 마이그레이션까지 갱신하고 가상 계정 회귀 fixture 옵션을 추가했다. 기존 검증 서버 77388·72682를 종료하여 메모리의 검증 계정·키·데이터를 정리했으며, 새 로컬 서버 세션 97324를 실행했다. http://127.0.0.1:5187/api/health가 ready=true·db/schema/runtime/capacity=true를 반환했고, 브라우저에서 새 개인 체험 생성과 운영판 표시를 확인했다. 이는 Supabase 호환 메모리 PostgreSQL 검증이며 운영 Supabase 연결 확인이 아니다. 운영 마이그레이션·외부 서비스·실제 고객 업무 효과·커밋·푸시·원격 배포는 범위 밖으로 미실행했다.

## 회차 2 — 필수 개선 로컬 반영 확인 (2026-09-19)

사용량 조회는 일반 사용 가능, 주간 사용률 51%(남은 비율 49%)를 반환했다. 회차 1 이후 소스·화면에서 추가로 확인할 후보는 도구 실행 실패 시 성공 결과까지 실패로 분류해 재기록하는 경로, 500건 범위 밖의 원본 판단 사건 접근, 소유계정이 없는 이전 신청의 명시적 권한 복구 경로다. 아직 재현·대안 비교를 마치지 않았으므로 구현 항목으로 확정하지 않았다. 미완료 항목을 우선 분석하고 필수 문제가 확인되면 최대 5개를 선정한다.

2회차는 실제 재현한 필수 문제 4개를 선정했다. 계산 성공 후 저장 오류를 주면 화면이 성공 POST 뒤 실패 POST를 추가 전송했다(신규 회귀 검사 1개 실패로 확인). 계산 결과·저장 상태를 분리하고 같은 실행 식별자로 재시도하며, DB 기록·성공 사용량·영수증을 단일 트랜잭션으로 묶는다. 실패 실행만 1회인데 성과·도구·부서·정직 4개 실제 API가 509초 절감으로 계산했다. 성공 기준선 인정과 전체 시도 비용을 분리하면 해당 fixture의 순변화는 -91초이며, 성공 1회와 실패 1회는 1,078초가 아니라 478초여야 한다. 501건 원본 중 가장 오래된 검토 대기 1건은 집계에만 있고 GET/화면에서 찾을 수 없어 서버 필터·안정 커서·군집 원문 독립 조회를 추가한다. 기존 소유계정 NULL 신청은 관리자만 읽고 참여 승인 409, 직접 owner 변경 42501이므로 관리자 명시 확인·이관 경로와 감사·동시변경 방지를 추가한다. 자동 소유자 추정·LIMIT 상향·실패행 삭제는 각각 권한 오인·문제 재발·실제 비용 누락 때문에 제외한다.

현재 계산/저장 분리 후 최초 화면 회귀 1개는 통과했다. 동일 실행 저장 재시도·한도 원자 처리(0011), 관리자 소유계정 복구(0010), 사건 조회·성과 계산은 구현 중이며 아직 전체 검사·빌드·실제 화면 검증을 하지 않았다. 기존 5187 서버는 회차 1 빌드를 제공하므로 이번 변경의 로컬 반영 증거로 사용하지 않는다.

첫 통합 검사 1,624개 중 1개는 미사용 event_list 응답 정보를 발견해 화면의 미리보기 범위에 연결했다. 다음 통합 검사 1,625개 중 1개는 기존 모의 DB에 새 원자 저장 RPC가 없는 문제였으며 모의 계약을 갱신했다. 린트와 클라이언트·Worker 빌드는 통과했다. 교차 검토에서 응답 유실 후 도구 중단 시 영수증 확인이 막히는 조기 409와, SPA 이동 시 미저장 식별자가 사라지는 문제를 재현했다. 조기 중단은 RPC에 위임하고, 파일 원문 없이 계정·개별 체험 공간별 해시 범위의 탭 메모리 기록 큐를 추가했다. 해당 변경 후 전체 재검사가 남아 있다.

브라우저 localhost:5188에서 미확인 신청의 활성 계정 명시 지정과 감사 이력 1건을 확인했다. 총 602개 중 과거 case-event-1을 검색하고 타당성 근거를 저장했으며, 군집 101건의 두 번째 페이지에 원문 1건이 표시되고 모달에서 열렸다. 390px 원문 모달은 문서 너비 390px로 가로 넘침이 없었다. 검증용 서버 8427을 종료해 해당 가상 데이터·키를 정리한 뒤 최신 코드로 재시작하여 도구 저장 재시도를 확인 중이다. 실제 다중 연결 DB 잠금 경쟁·운영 계정은 검증하지 않았다. 별도 교차 검토에서 후속 과제 201건의 과거 피드백 연결이 전역 200건 제한에 누락되는 문제를 재현했으며, 다음 회차의 필수 후보로 보존한다.

도구 경로는 계정/체험 공간 변경 직후 이전 탭의 재시도가 새 계정 기록으로 저장되지 않도록 최초 payload의 run_scope를 POST에서 재검증했다. UI·실제 PostgreSQL 관련 37개 검사와 최종 전체 107파일 1,637개(일반1,631+규모6)가 통과했으며 린트·클라이언트122모듈/Worker 빌드·git diff --check도 통과했다. 브라우저에서는 기존 가상 정산 CSV로 계산 성공·DB 커밋 후 응답 유실을 재현했다. 결과 다운로드는 활성, 새 계산은 비활성이었고 내부 신청서 화면 이동 후 돌아와 같은 기록을 재시도했다. 최종 화면은 총2회=성공1+기존실패1, 한도2/3으로 중복 기록이 없었다. 이때 시험용 접수번호 형식이 조회 규칙과 달랐으므로 fixture의 두 번호를 유효 형식으로 맞췄다(내부 이동·복구 검증 자체는 실행됨).

검증 서버8427·19127·46934와 이전 정상서버97324를 종료하여 해당 메모리 검증 자료·서명키를 정리하고 정상 로컬서버62157을 시작했다. http://127.0.0.1:5187/api/health의 준비 상태와 새 개인 체험 시작을 확인했다. 현재 화면 탭2만 결과물로 남겼고 가상계정 탭4는 닫았다. 파일 내용은 탭 기록 큐에 저장하지 않으며 새로고침/탭 종료 시 큐는 유지되지 않는다는 안내·경고가 있다. 아직 운영 마이그레이션·운영 계정 연결·실사용 생산성 효과·다중 연결 DB 경합은 검증하지 않았고, 커밋·푸시·외부 배포는 수행하지 않았다.

## 회차 3 — 필수 개선 로컬 반영 확인 (2026-09-19)

회차 사이 사용량은 일반 사용 가능, 주간 60% 사용(40% 남음)이었다. 첫 필수 항목은 앞서 실제 PostgreSQL GET으로 확인한 과거 피드백의 후속 과제 연결 누락이다. 전역 요약 200건은 유지하고 현재 페이지 사건·표본의 연결 과제만 별도로 조회해 보완한다. 제한 상향이나 전체 과제 로딩은 재발·응답량 증가 때문에 제외한다. 동시에 신청→협의→제작→베타→인수·성과 및 피드백→과제→실험→재확인의 연결 단절을 읽기 전용으로 점검하고, 실제 재현된 필수 문제만 추가 선정한다. root는 화면·현재 코드의 같은 유형 문제를 검토한다.

3회차의 필수 항목은 총 5개로 확정했다. 실제 베타 API에서 확정 기준 2개 중 필수 안전을 빼고 1개만 보내면 201/통과였고, 결과 INSERT를 실패시키면 API500인데 통과 회차·결과0개가 남았다. 현재 기준 revision·전체 ID·DB 안전 속성을 검사하고 회차·결과·상태·영수증을 원자 저장한다. UI도 재채점 없이 동일 실행 ID로 저장을 확인하도록 보완한다. 실험에서는 현재 승인 뒤 historical2002/shadow2001/limited2000년 역순 기간으로 확대200이 재현됐다. historical의 과거 자료 기간은 허용하되 실제 shadow/limited는 승인과 단계 시간 순서를 검증한다. 확대 뒤 원신고자가 미해결을 확인해도 rollback409인 문제는 최초 확대 근거를 보존하면서 새 롤백 결정을 추가하는 방식으로 보완한다. 임의 확정 기록 덮어쓰기·기준 전체 로딩 생략·성공 회차만 우선 저장하는 대안은 제외했다.

과거 후속 과제 연결은 전용 PostgreSQL·UI 및 기존 관련 8파일69검사, 상수 정리 후 핵심3파일19검사가 통과했다. 전역 요약200건·작은 데이터2RPC 계약을 유지한다. 아직 3회차 전체 검사·빌드·실제 화면 검증·로컬 반영은 남아 있다. 브라우저 디자인 점검 중 도구 화면의 일부 세부 CSS가 사용되지 않는 옛 App.css에만 남아 있는 정황을 확인했지만, 이번 5개 필수 범위에는 추가하지 않았다. 다음 회차에서 실제 렌더링 근거로 재검토한다.

베타는 0012_beta_round_receipts.sql에서 현재 확정 기준·revision·안전 속성을 검증하고 회차·결과·감사·영수증을 원자 저장한다. BetaPage는 계산과 저장을 분리해 응답 유실 시 같은 run_id로 확인하며, 계정/체험 범위가 바뀌면 이전 비동기 결과가 새 화면에 표시되지 않도록 세션을 분리했다. 확실한 미저장 기준 변경에만 다시 준비할 수 있다. 실험은 현재 승인 이후의 실제 측정·단계 순서를 저장과 확대 양쪽에서 검사하며, 확대 후에는 원래 결정을 보존한 추가 롤백 기록만 허용한다. API 필드 오류가 일반 문구로만 나오는 문제도 발견하여 모달에 구체적인 사유와 입력값을 유지하도록 보완했다.

localhost:5188의 메모리 검증 계정으로 전역200건 밖의 과거 피드백 연결, 베타 커밋 뒤 응답 유실→내부 화면 이동→동일 결과 재시도(최종1회차·결과2개), 확대 뒤 롤백 추가와 최초 근거 보존을 실제 브라우저에서 확인했다. 승인 전 Shadow와 중첩 Limited 기간은 구체적 사유로 거절되고, 인접한 Shadow/Limited 경계는 허용되어 확대 선택이 활성화됐다. 390px 입력 모달의 문서/모달 너비390px로 가로 넘침이 없었다. 첫 전체 실행에서 구형 베타 계약을 검사하던8개가 실패했고 RPC 계약으로 갱신했다. 최종 전체113파일1,699개(일반1,693+규모6), 린트, 클라이언트123모듈·Worker 빌드, diff --check가 통과했다.

검증 서버20597·6539와 이전 정상서버62157을 종료하여 메모리 테스트 계정·키·데이터를 정리했다. 최신 정상 로컬서버29498을 시작했으며 http://127.0.0.1:5187/api/health의 ready 및 db/schema/runtime/capacity=true, 브라우저 개인 체험 생성과 메뉴 진입을 확인했다. 운영 Supabase·실제 PostgreSQL 다중 연결 경합·실제 AI 제어·실사용 생산성 효과는 미검증이다. PGlite RPC 큐의 동시 요청 테스트는 다중 DB 세션 잠금 검증을 대체하지 않는다. 커밋·푸시·외부 배포는 하지 않았다.

## 회차 4 — 필수 개선 로컬 반영 확인 (2026-09-19)

사용량은 일반 사용 가능, 주간69% 사용(31% 남음)이었다. 3회차 교차 검토에서 ToolPage의 계산/저장 중 계정 범위가 바뀌면 이전 결과가 새 화면에 나타나거나 새 pending 큐가 덮이는 UI 경합을 실제 지연 응답 검사로 확인했다. 서버 run_scope 검증은 쓰기를 거절하므로 권한 우회로 기록하지 않는다. BetaPage와 같은 수명 단위 분리·동일 run_id 조건부 큐 삭제를 우선 검토한다. 추가 후보는 OverrideLoop의 주소/뒤로가기와 화면 선택 불일치, 실제 기능 화면의 누락된 세부 스타일이며 재현된 문제만 선정한다. 기존 기능·운영 자료를 유지하고 로컬에서만 변경한다.

필수 개선 5개를 선정했다. (1) 도구 계산·저장 비동기 scope 경합은 수동 state 일괄 초기화보다 scope별 컴포넌트 수명과 실행ID 조건부 큐 변경을 선택했다. (2) OverrideLoop의 hash/back/forward/Router Link 불일치는 양 경로의 실제 App 회귀에서 10개 결함을 확인했다. hashchange만 구독하면 Router pushState를 놓치므로 Router 위치를 기준으로 통일한다. (3) 도구에서 비용 숫자와 ‘성공 확인 없음’, 한도·파일 크기·로컬 계산 안내가 붙어 보이는 것을 실제 브라우저로 확인했다. 폐기된 App.css 전체 복원 대신 현재 Microsoft CSS에 필요한 구조·판정 구분만 복원하며 베타의 같은 누락도 포함한다. (4) 조회 A 대기→B 완료→늦은 A로 화면과 URL이 A로 돌아가는 TrackPage 경합은 공통 useApi 보호 및 ticket별 수명 분리로 보완한다. (5) 현장 피드백 재조회403 뒤 이전 직원 원문·관리자 안내 폼이 유지되는 경로는 401/403/404/410 시 공통 GET 캐시를 비우고 일시적 네트워크/503 실패의 정상 복구는 유지한다. 모두 서버 권한 우회와는 구별한 화면 정확성·정보 노출 방지 문제다.

도구 배치와 기존 디자인 계약 13개 검사가 통과했다. 브라우저에서 도구 수치/설명8px·한도 항목12/24px 간격, 390px viewport의 문서너비375px로 가로 넘침 없음과 한도 세로 배치를 확인했다. 아직 계정 경합·탐색·조회·권한 회수 통합 검증과 최종 빌드·로컬 갱신이 남아 있다.

Tool은 scope/slug별 컴포넌트와 이름 저장 키를 분리했고 legacy 공용 이름을 자동 이관하지 않는다. Tool/Beta 모두 재진입 후 원래 저장의 확인을 구독하며 run_id당 최신 상태 조회1회, 늦은 실패의 이미 확인된 큐 재생성 차단을 적용했다. 두 경로 관련62개 검사 통과. Track 본문·하위GET6개와 Thread/조회 쓰기9개에 리소스·생명주기 보호를 적용했으며 신규37개 포함146개 통과. OverrideLoop Router 탐색 및 관련102개 통과. 권한회수 hook/피드백UI13개와 독립 관련56개도 통과했다. 이는 최종 전체 검사 결과가 아니다.

브라우저에서는 /override#events→#experiments→뒤로→앞으로의 주소와 aria-current 일치, 건너뛰기Enter 후 hash 유지와 activeElement=main을 확인했다. 조회 A→B 수동검색 후 B문서 링크가 일치했고 기존 replace 검색 이력과 앞으로가기 B복원도 확인했다. 베타 가상 파일 채점/응답유실 재시도 후1회차만남음, Tool 내부조회화면 이동·복귀 후 동일 실행 재시도에서 총2회=성공1+기존실패1·한도2/3을 확인했다. 베타 판정/근거 카드 데스크톱·390px 문서너비375px를 확인했다. 가상 계정을 비활성화한 뒤 피드백 새로고침에서401 안내·caseHeadings0·caseForms0도 확인했다.

검증 중 인증 확인 직후 DB 조회에서 계정이 비활성화되면 SQLSTATE28000이 일반503으로 전달되는 경계를 발견하여 항목5 안에서 DB/HTTP 오류 분류를 보완 중이다. 초기 개발 StrictMode 중첩 조회와 혼동하지 않도록 fixture는 초기2개 성공 조회 뒤 가상계정만 회수하며, 이미 취소된 응답에는 회수 hook을 실행하지 않는다. 또한 Thread의 저장 중 닫기→재열기→새초안이 늦은 성공으로 지워지는 기존 결함을 교차 재현하여 항목4의 동일 폼 수명 문제로 보완 중이다. 현재 검증 서버89365는 메모리만 사용하며 종료 후 자료를 정리한다. 후속 후보는 한 리소스에서401을 받은 뒤 다른 부모 리소스의 기존 캐시도 함께 무효화해야 하는 전역 세션 경계다. 아직 그 후보의 재현·구현을 완료했다고 주장하지 않는다.

전체119파일1,846개(일반1,840+규모6), 린트, 클라이언트124모듈·Worker 빌드가 통과했다. 이후 DB 경계 검사2개를 더 추가하여 별도4파일50개도 통과했다. 브라우저에서 실제 질문1개 저장→새초안작성→취소 후 기존질문1개 보존을 확인했다. 질문 저장 중 입력/닫기 잠금은 신규16개 지연응답 검사로 확인했으며 브라우저에서 인위적 지연은 주지 않았다. 검증 서버89365·66255·31033·37111은 종료해 메모리 검증 자료와 키를 정리했다. 아직 일반500/503으로 DB 인증 오류를 바꾸는 같은 유형의 API catch 보완과 그 이후 최종 빌드·로컬 서버 갱신이 남아 있다.

다음 회차 후보인 전역 세션 문제는 실제 React 화면·공통API·GET훅에서 fetch만 모의 처리해 재현됐다. 피드백 새로고침401 후 자식원문은 사라지나 반복문제 메뉴에서 부모캐시의 기존 원문과 수정버튼이 다시 나온다. 부모GET은 최초1회였다. 서버 권한 우회가 아니라 이미 받은 자료와 입력 상태의 UI 잔류다. 페이지 강제새로고침은 미확정 도구/베타 영수증 큐를 잃으므로 제외하고, 세션 세대별 차단·재확인·보호 트리 재마운트를 다음 회차에서 검토한다.

같은 오류를 삼키던 API 최외곽·중첩 DB catch를 공통 분류로 보완했다. 인증/권한 오류만401/403으로 전달하고 일반500/503 및 의도된 JSON파싱·선택적 감사 fallback은 유지한다. 권한 오류 후 선행 제작 기록을 삭제하지 않으며 미저장이라고 보증하지 않는다. 실제 PG9개와 실제 핸들러 경계 포함 신규3파일83개가 통과했다. 최종 전체121파일1,925개(일반1,919+규모6), 린트, 클라이언트124모듈·Worker 빌드가 통과했다. 중간 실패는 만료 토큰의 구형428기대값, 최초 lazy import의1초대기, 저장 상태보다 늦게바뀌는 계산중버튼문구 검사, 새검사의회수트리거 조건을 각 계약에 맞춰 수정해 재실행했다. CRLF를 공백으로 오인하지 않는 git diff --check도 종료0이다.

이전 정상서버29498 종료 후 최신 정상서버83712로 갱신했다. http://127.0.0.1:5187/api/health의 ready 및 db/schema/runtime/capacity=true, 실제 브라우저 개인 체험 생성→개선실험 화면 진입을 확인했다. 결과물 탭2만 남기고 검증탭은 닫았다. 데이터는 로컬 메모리이며 서버 종료로 정리된다. 운영계정·실제동시DB세션·실사용생산성은 미검증이며 커밋·푸시·외부배포는 하지 않았다.

## 회차 5 — 필수 개선 로컬 반영 확인 (2026-09-19)

일반 사용 가능, 주간80%사용(20%남음)을 확인했다. 재현된 필수 문제2개만 선정한다. (1) 한 자식조회401 뒤 부모원문·모달·토스트가 남는 문제는 공통 세션세대/업무요청차단, 보호트리언마운트와 경량인증재확인으로 해결한다. 부모만reload하면 직접API·로컬상태를 놓치고 강제새로고침하면 미확정영수증을 잃으므로 제외한다. 403/404/일시장애는 개별리소스처리를 유지하며 늦은옛200/401이 새세션에 영향을 주지 않게 한다. (2) A에서신청초안자동저장→B빈목록으로재진입시 A제목이보이고 이어쓰기시 이름·연락처가복구되는 UI를실제컴포넌트/모의API로재현했다. 서버확정scope별초안과폼수명을분리한다. 소유불명기존공용키는자동이관·표시·삭제하지않고보존한다. 새체험공간의이전초안노출도같이검증한다. Tool/Beta미확정큐는원래scope에보존하며 DB스키마추가·운영변경은하지않는다.

보호 GET /api/session은 기존 인증 middleware·scoped DB SELECT 1 성공 뒤 mode/opaque scope만 반환한다. 전용 실서명/PGlite 40개와 관련 55개가 통과했다. 공통 client는 현재 세션 401/428만 전역 차단하며 늦은 응답·JSON 읽기·취소된 요청을 세대별로 검증한다. Gate가 ToastProvider까지 언마운트하고 명시적 재확인 뒤 새 세대에 열어준다. client/기존 조회·진입 6파일 100개, 독립 UI 14개, 실제 Override 부모/원문 모달/토스트 3개와 관련 62개 검사가 통과했다. Apply는 세대·scope별 폼 수명과 초안 키를 사용하고 자동저장·유사 검색·제출 완료 콜백을 격리했다. 신규 UI 20개 포함 관련 6파일 146개가 통과했다. 명시 reset은 현재 scope 초안과 표시 이름만 삭제하고 다른 scope는 보존한다.

최종 전체 126파일 2,038개(일반 2,032+규모 6), 린트, 클라이언트 125모듈·Worker 빌드, diff 검사가 통과했다. 중간 형식 검사 1건은 session의 비GET 위임 핸들러도 요청별 환경을 명시적으로 전달하도록 수정했다. 빌드와 전체검사를 병렬 실행해 산출물 교체 중 Worker import 1건이 실패한 것은 빌드 완료 후 전체 재실행하여 통과했다. 브라우저에서는 계산·DB 저장 뒤 응답 유실→다른 화면에 신청 초안 작성→피드백 401→보호 트리 제거→첫 재확인 401 차단 유지→두 번째 재확인 성공→기존 도구 기록 재확인 순서를 실행했다. 최종 총2회=성공1+기존실패1, 미확정 영역0, 새 계산 활성으로 중복 기록이 없었다. 같은 계정의 이름·연락처·제목 초안 복구를 확인한 뒤 UI로 이번 검증 초안만 제거하고 재진입해 안내가 사라진 것을 확인했다.

검증 서버48648과 이전 정상서버83712를 종료하여 메모리 가상계정·서명키·자료를 정리했다. 최신 정상서버74700의 http://127.0.0.1:5187/api/health 준비 상태와 브라우저 개인 체험 생성→개선 실험 메뉴 진입을 확인했다. 탭2를 결과물로 남기고 검증 탭9는 닫았다. 실제 운영 계정·다중 연결 DB·실사용 생산성 효과는 미검증이며 커밋·푸시·외부 배포는 하지 않았다. 다른 탭에서 쿠키가 바뀌어 401 없이 새로운 계정의 200을 받는 경계는 다음 회차 후보로 재현 중이다.

## 회차 6 — 필수 개선 및 배포 전 검증 (2026-09-19~20)

주간 사용86%(14%남음), 일반 사용 가능을 확인했다. 첫 필수 항목은 다른 탭의 유효 계정/쿠키 변경이다. 실제 서명·PGlite 경로에서 A 세션 확인 뒤 B GET200/POST200이 반환돼도 UI scope는 A였으며, A 질문이 B 명의 또는 B 체험 DB에 저장됐다. 실제 Gate·Thread UI에서도 A 부모/초안과 B 하위 자료가 함께 표시되고 B로 저장됐다. RLS 우회는 아니지만 입력 의도와 귀속이 다르다. 보호 API 및 체험 초기화에 X-Ilson-Scope 사전조건을 필수화해 현재 인증 DB 범위와 불일치하면 handler·쓰기·할당 전에 거절한다. focus/BroadcastChannel 확인만으로는 확인 후 저장 사이 변경을 막지 못하므로 제외했다. 직접 API·구형 탭도 /session 확인이 필요하도록 예제와 문서를 갱신한다.

두 번째는 오래된 검토 판정의 덮어쓰기다. 수용 GET→다른 담당자의 반려 POST→옛 수용 폼 저장이 모두200이고 최종 판정이 수용으로 되돌아가는 것을 실제 PostgreSQL로 재현했다. 초 단위 updated_at 비교 대신 단조증가 revision과 DB 원자 비교·저장을 적용한다. 개별 검토 및 같은 변경을 하는 일괄 보류·재신청 경계를 함께 확인한다. 성과 확인 시 보는 수치와 저장하는 수치의 불일치는 추가 후보로 실제 사용 경로를 재현 중이며 아직 구현 항목으로 확정하지 않았다.

세 번째 필수 항목은 사용자가 지적한 큰 화면의 중앙 집중 배치다. 1920px 화면에서 본문1280px·왼쪽313px를 실측했다. 공통 작업영역의 고정 상한을 제거하고 상단바·본문·목록과 상세의 기존 격자를 화면 폭에 맞췄다. 긴 설명문660/760px와 대화상자 읽기 폭은 유지한다. 변경 후 같은 화면에서 본문1841px·좌우 작업여백32px, 2560px 신청화면의 양쪽열1234px, 390px 화면 문서375px·본문335px로 가로 넘침이 없음을 DOM으로 확인했다. 모든 페이지의 실제 배치 검증을 완료했다고 보고하지 않는다.

계정범위 사전조건은 실제 브라우저에서 A 피드백 조회 후 가상 유효계정B로 전환해 확인했다. 구범위409 직후 보호화면이 사라지고 명시적 재확인 뒤 B의 표시이름과 역할이 나타났다. 기존 검증서버58347을 종료하여 메모리자료를 정리했다. 검토CAS 신규PG21개·UI12개와 관련묶음137개/112개, 지정된0013 loader6파일67개가 통과했다. 전체 빌드125모듈·Worker 및 린트도 통과했다. 전체 회귀검사와 두 탭 실제 판정충돌 검증, 최신 정상서버 갱신은 진행 중이다. 현재 정상서버74700은 이전0012 DB이므로 신규검토목록503이 발생하며 최종갱신 전 상태임을 구별한다. 커밋·푸시·외부배포·운영마이그레이션은 수행하지 않는다.

사용자가 2026-09-20에 기존 작업을 마무리하고 일손만 커밋·푸시·배포하도록 범위를 변경했다. 두 탭 검토 충돌에서 다른 탭의 보류 저장 뒤 이전 수용 저장이 거부되고, 초안 보존·최신 근거 읽기·명시적 재확인 뒤 저장되는 경로를 실제 브라우저로 검증했다. 검토 목록과 상세, 부서 요약과 보조 영역, 비교값의 열 배치를 보완했고 비어 있는 부서 보조 영역은 숨긴다. 넓은 화면의 작업 폭과 좁은 화면의 단일 열을 구분하며 긴 문장 읽기 폭은 유지한다.

배포 전 검사에서 0006의 비슈퍼유저 역할 속성 변경, 함수 소유권 이전에 필요한 임시 CREATE 권한, NOINHERIT 계정의 소유권 이전 후 권한 철회 누락을 재현했다. 안전 속성 검사와 ACL/소유권 이전 순서로 수정하고 0000~0013 전체 체인·익명 RPC 거부·강제 실패 롤백을 PostgreSQL 호환 엔진에서 검증했다. 최종 lint → build → test를 순차 실행하여 린트 0, 클라이언트 125모듈·Worker 빌드 0, 133개 파일·2,126개 테스트(일반 2,120 + 규모 6), CRLF 안전 diff 검사 0을 확인했다.

성과 수치 수정 뒤 이전 확인 표기가 유지될 수 있는 문제는 배포 이전 커밋에서도 재현된 기존 제한이다. 이번 배포에서 이를 해결했다고 주장하지 않는다. 실제 외부 AI 제어, 고객 업무 효과, 운영 다중 DB 연결 부하는 검증 범위에 포함하지 않았다. 운영 인증과 DB 반영 결과는 배포 검사 결과가 확정된 뒤 별도로 기록한다. `portfolio` 저장소·Pages·Supabase 프로젝트는 변경 대상이 아니다.

## 문구 정리 및 운영 연결 검증 (2026-09-20)

사용자는 중복 설명·홍보 문구·장식 제목을 삭제하고 버튼명·입력 안내·오류·보안·데이터 안내·서비스 이름을 유지하는 범위를 승인했다. 공통 상단 중복 메뉴, 반복 단계 소개, 업무 현황의 홍보 영역과 하단 설명을 제거했다. 운영 메뉴·검토 상세 필터·비교 낱말 목록은 접힌 상태로 제공하며 기존 경로와 기능은 유지했다. OverrideLoop와 업무 화면의 반복 문구를 정리하고 실제 판단 원문·근거·수치의 의미는 남겼다. 브라우저에서 검토·비교·업무 현황·OverrideLoop를 390/1280/1920px로 확인하여 가로 넘침 0, 콘솔 오류 0을 확인했다. 모바일 검토 목록 시작 위치는 1529px에서 766px로 줄었고 상세 필터 펼침·부서 선택·낱말 목록 토글을 실행했다.

일손 Supabase의 기존 86개 표·97개 행을 로컬 비공개 백업으로 보존한 뒤 0006~0013을 실제 DB에서 단일 트랜잭션으로 시험 적용·롤백하고 같은 묶음을 확정했다. 적용 직후 기존 열 기준 자료 차이 0, 준비 상태 0013, 확인 대상 RPC의 익명·일반 계정 실행 권한 없음과 서비스 계정 권한을 확인했다. 운영 배포 뒤 health 503을 발견했으며 서버 키 재설정으로는 해결되지 않았다. 실제 workerd에서 지원하지 않는 fetch redirect:error가 요청 전 TypeError를 발생시키는 원인을 재현했다. 서버 4곳을 manual로 바꾸고 비정상 응답 거절을 유지했다. 회귀 검사는 정상 RPC 200과 301/302/303/307/308 거절, 리디렉션 대상 호출 0회를 검증한다. 수정본 배포 뒤 운영 health 200과 Supabase 연결을 확인했다.

운영 사이트의 격리된 임시 공간 2개에서 세션 확인, 5개 업무 API 조회, 실험 생성·승인·근거 기록·동일 요청 재시도·공간 간 분리를 실행했다. 요청 범위 교차는 409였고 재시도는 동일 기록을 반환했다. 검증 후 생성한 공간의 정확한 식별자를 대조하여 두 스키마와 등록 행만 제거했다. 배포 전부터 있던 자료를 테스트용으로 수정하지 않았다. 최종 로컬 검사는 135개 파일·2136개 테스트(일반 2130+규모 6), 린트, 클라이언트 125모듈·Worker 빌드 모두 통과했다. 390개 소스 파일의 지정 비밀정보 패턴 검사에서 발견 0건이며 전체 보안 보증을 의미하지 않는다. 릴리스 대상은 GitHub k95691368-ship-it/ilson과 기존 Cloudflare Pages ilson으로 확인했다.
# TypeScript 1차 전환 — AX 기획·개발 포트폴리오 기반 (2026-09-26)

기존 React/Vite·Cloudflare Pages·Supabase와 Microsoft 디자인은 유지했다. 검토·협의·성과 입력·베타 근거·인계의 공용 규칙 5개, 검토 API, 공통 API 통신, 인계 화면을 TypeScript로 전환했다. 검증 전 JSON은 unknown으로 취급하며 기존 서버 입력 검증, 사람의 승인, 세션 범위, 동시 변경 사전조건, 동일 요청 재시도는 유지했다. 인계 GET은 화면에서 소비하는 필드만 실제 검사한다. 나머지 응답 전체를 검증했다고 주장하지 않는다. strict 타입 검사를 빌드와 CI에 추가했고 로컬 메모리 서버 및 기존 소스 탐색 검사가 TS/TSX를 포함하도록 바꿨다.

최종 로컬 lint → typecheck → build → test → npm audit를 순차 실행했다. 154파일 2,367개(일반 2,361 + 규모 6) 통과, 클라이언트 126모듈과 Worker 빌드 성공, 알려진 의존성 취약점 0건이다. 타입 단언은 이 런타임 검사 수에 포함하지 않는다. 핵심 업무 흐름 집중 검사 4파일 81개도 통과했다. 공용 규칙의 원본 JS와 TS를 4,057개 입력 조합으로 비교해 반환값 및 예외 클래스가 일치했다. 브라우저의 독립된 로컬 체험에서 대안 누락 저장 차단, 수용 판정 저장, 새로고침 후 같은 신청서의 판정·근거 보존, 인계 GET 표시와 선행 조건 미충족 차단을 확인했다. 문서/스크롤 폭은 둘 다 1,265px, 해당 검사 중 콘솔 경고·오류는 0건이었다. CSS 변경은 없으며 산출물 CSS 해시는 index-BRFuXbZt로 이전과 같다.

전체 JavaScript 전환은 하지 않았다. allowJs=true/checkJs=false이므로 미전환 코드는 strict 보증 범위 밖이다. Python 서비스·새 AI 기능·DB 마이그레이션은 추가하지 않았다. 실사용 생산성 효과, 외부 AI 제어, 운영 다중 연결 부하와 모든 페이지의 브라우저 검증은 수행하지 않았다. 로컬 실행은 Node 24.18.0이며 CI Node 22 결과와 운영 배포 결과는 배포 시 별도 확인한다. 이 기록 시점에 커밋·푸시·배포는 아직 하지 않았다.
